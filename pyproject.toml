[project]
name = "turok"
version = "0.1.0"
description = "CLI torrent search via Jackett"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "requests>=2.28",
    "beautifulsoup4>=4.12",
]

[project.scripts]
turok = "main:main"
